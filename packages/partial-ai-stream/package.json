{
    "name": "partial-ai-stream-lib",
    "version": "0.1.0",
    "devDependencies": {
        "@types/node": "^20.6.0",
        "typescript": "^5.2.2"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    },
    "dependencies": {
        "openai": "^4.6.0",
        "zod": "^3.22.2",
        "zod-to-ts": "^1.2.0"
    },
    "main": "src/index.ts",
    "types": "src/index.ts",
    "scripts": {
        "build": "tsup src/**/*.ts --format esm,cjs --dts --clean",
        "dev": "npm run build -- --watch"
    }
}
