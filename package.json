{
  "name": "partial-ai",
  "version": "0.1.0",
  "module": "index.ts",
  "type": "module",
  "workspaces": [
    "src",
    "website"
  ],
  "scripts": {
    "clean": "rm -rf lib",
    "build:lib": "tsc -p tsconfig.lib.cjs.json && tsc -p tsconfig.lib.esm.json",
    "build:website": "tsc -p tsconfig.website.json",
    "build": "npm run build:lib && npm run build:website"
  },
  "author": "Yanael Barbier",
  "license": "MIT",
  "files": [
    "lib/**/*"
  ]
}
